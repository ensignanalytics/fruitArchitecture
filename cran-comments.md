## Release status

This is the initial development release of fruitArchitecture.

## Required edits before submission

* Replace placeholder maintainer email in DESCRIPTION.
* Replace placeholder GitHub URLs in DESCRIPTION.
* Confirm package title, terminology, and metric equations.
* Run `devtools::document()` and `devtools::check()`.
* Run checks on Windows, macOS, and Linux.
* Add citations and a CITATION file before public release.
